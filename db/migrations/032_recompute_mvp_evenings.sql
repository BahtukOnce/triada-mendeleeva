-- Маркер пересчёта рейтинга (без SQL-стейтментов).
-- Колонка mvp_evenings (MVP вечеров) была добавлена миграцией 016 уже после
-- наполнения rating_cache импортом, поэтому у всех игроков осталась 0.
-- Имя файла содержит «recompute» — run_migrations() поймает это и один раз
-- выполнит rating_recompute_all(), который заново посчитает MVP вечеров
-- (игрок с максимальной суммой итогов за игровой день) для всех игроков.
