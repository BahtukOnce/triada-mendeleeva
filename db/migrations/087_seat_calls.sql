-- Версия игрока («протокол участника»): кого из соседей по столу он считает красным, а кого
-- чёрным — например «5ч, 4к». Решение руководителя (сентябрь 2026): версию вправе оставить
-- каждый участник, судья вносит её в протокол и сам смотрит при выставлении допов —
-- автоматически баллы не начисляются. Своё место отметить нельзя.
--
-- Хранится строкой «4r,5b»: место + r (красный) / b (чёрный), по возрастанию мест, NULL — версии
-- нет. Разбор и показ — seat_calls_parse()/seat_calls_chips() в inc/helpers.php.
-- Повторный прогон безопасен: ADD COLUMN раннер пропускает (1060).

ALTER TABLE game_seats ADD COLUMN calls VARCHAR(40) NULL;
