-- Пароль задаётся сразу в заявке (одна форma). Храним хеш до приёма руководителем;
-- при приёме создаётся аккаунт и хеш обнуляется. Активация по ссылке больше не нужна.
ALTER TABLE club_applications ADD COLUMN password_hash VARCHAR(255) NULL;
