-- Маркер пересчёта (без SQL-стейтментов): перестроить историю ELO под новую формулу из 085.
--
-- Миграция 085 не пересчитала ELO: хук в inc/db.php звал только rating_recompute_all(), а тот
-- историю ELO не трогает (в CLAUDE.md было написано иначе). Хук научен: «recompute» в имени —
-- рейтинг, «recompute» + «elo» — ещё и elo_recompute(). У этого файла в имени есть оба слова.
