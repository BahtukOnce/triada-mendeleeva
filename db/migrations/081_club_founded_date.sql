-- Дата основания клуба для «Летописи клуба» в «Зале славы». Решение руководителя: клуб основан
-- в день первой игры летописи — 8 апреля 2023. Дальше меняется в «Админке → Правила и тексты».
-- Заполняем только пустое значение: если дату уже вписали руками, миграция её не перетрёт.
-- Идемпотентно: повторный прогон ничего не меняет.

INSERT INTO settings (k, v) VALUES ('club_founded', '2023-04-08')
ON DUPLICATE KEY UPDATE v = IF(v IS NULL OR v = '', VALUES(v), v);
