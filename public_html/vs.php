<?php
require dirname(__DIR__) . '/inc/bootstrap.php';
// «Очная ставка» и «Дуэль» делали одно и то же: сравнение двух игроков и счёт личных встреч.
// По решению руководителя страницы объединены в /versus.php — сюда же переехало сравнение
// показателей, ролей и мест за столом. Старые ссылки и закладки ведём туда же.
$q = [];
foreach (['a', 'b'] as $k) {
    if (!empty($_GET[$k])) {
        $q[$k] = (int)$_GET[$k];
    }
}
redirect('/versus.php' . ($q ? '?' . http_build_query($q) : ''));
