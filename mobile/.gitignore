# зависимости и сборки — не в git
node_modules/

# Android build outputs
android/.gradle/
android/build/
android/app/build/
android/captures/
android/local.properties
android/.idea/
android/*.iml
android/app/*.iml

# Ключ подписи для сайдлоада КОММИТИМ намеренно (keystore/triada-release.jks +
# keystore.properties) — иначе обновления не встанут поверх старой версии.
# Play-ключи (если появятся) сюда класть НЕЛЬЗЯ — только в GitHub Secrets:
release.keystore
upload*.jks
play*.jks

# firebase (добавляется владельцем для push)
android/app/google-services.json

.DS_Store
